# 零奈解题失败复盘

本文独立记录真实解题日志中的失败原因、当前改动覆盖范围和后续优化方案。日志统计基于当前仓库 `artifacts/` 下的 Codex transcript，属于一次运行样本，不等同于整体题库成功率。

## 样本统计

当前样本包含 56 份 Codex Worker transcript：

| 指标 | 数量 | 说明 |
| --- | ---: | --- |
| `exit_code=124` | 38 | 进程达到外层硬超时，未正常完成结构化收尾 |
| `exit_code=0` | 18 | 进程退出成功，不代表 flag 已验证 |
| 模型元数据缺失告警 | 56 | `deepseek-v4-pro` 未注册，全部回退到默认元数据 |
| 工作区重置/输入丢失迹象 | 15 | 恢复后重新解压或重新寻找输入文件 |
| flag 验证相关记录 | 114 | 包含候选拒绝、参数错误和成功重放 |

主要瓶颈是执行边界和状态连续性，而不是单纯缺少题型工具。`exit_code=0` 只能说明 Harness 退出，必须结合证据 Artifact、checkpoint 和 `flag.verify` 结果判断是否完成。

## 失败原因

### 1. 执行边界和模型配置

- 日志普遍出现“Model metadata not found”，fallback metadata 可能导致 token 预算、上下文压缩和停止判断失真；
- 38/56 份 transcript 以超时结束，软截止没有稳定转化为结构化输出；
- 超时过程中即使已有有效分析，如果 checkpoint 没有及时写入，下一轮仍会重复计算。

### 2. 工作区和会话恢复

- 多份日志显示恢复后工作区为空，需要重新解压附件或重新定位输入文件；
- `codex_thread_id` 能恢复不等于题目文件、脚本和中间产物也被恢复；
- 会话状态和文件状态分叉后，会造成重复分析或错误结论。

### 3. Agent Loop 重复路线

- 失败路线在不同 Intent 间重复出现，最近一轮上下文不足以阻止相同命令再次执行；
- 长时间推理和重复命令没有同步形成新的事实或 checkpoint；
- 仅增加工具数量不能解决该问题，失败路线、路由指纹和唯一下一步必须进入下一轮上下文。

### 4. flag 证据和验证链路

- 候选 flag 被拒绝后仍可能围绕同一候选继续推理；
- `flag.verify` 曾缺少 `source_artifact_refs`，或指定不存在的验证脚本；
- 有些脚本能运行，但输出格式不符合“成功退出且只输出一个 flag”的合同；
- 模型文本不能作为完成条件，必须同时满足可信 Artifact、可重放脚本和双次一致验证。

### 5. 题目环境与调度状态

- Web 题没有靶机时曾被错误当成运行时失败，遮蔽了可以先做的本地分析；
- 题目组并行、WAITING_INPUT、手工验证和异常清理之间存在状态转换复杂度；
- 共享 SQLite 数据库和 API instance lock 会制造与业务逻辑无关的测试假失败。

## 当前改动与问题对应关系

| 问题 | 已有改动 | 仍需验证 |
| --- | --- | --- |
| 外层超时丢失进展 | `worker_runtime` 记录 thread/turn；`command_runner` 发送 SIGINT；增加软截止、动作预算和收尾宽限 | 超时样本是否都能产生 checkpoint，并被后续 Attempt 使用 |
| Blackboard 延迟同步 | `context_builder` 注入 live checkpoint；Worker MCP 实时读写事实和 checkpoint | 日志中是否稳定出现 `checkpoint.saved` |
| 重复工具路线 | `observer`、`route_fingerprint`、`manager` 记录失败路线并生成续跑目标 | 阈值触发后是否真正跳过原路线 |
| 错误 flag/幻觉 | `flag_validator` 和 `result_processor` 要求可信 Artifact 与验证结果 | 参数校验是否在派发前完成 |
| 无靶机无法开始 | `challenge_group_runner`、`hands_free`、`target_verification` 将靶机改为可选输入 | Web 题无靶机时能否完成本地分析并无损恢复 |
| 附件下载卡住 | `hands_free` 增加浏览器下载、远程请求、重试和硬超时 | 失败附件是否转人工处理且不阻塞整批导入 |
| 题目组吞吐不足 | `challenge_group_runner` 增加受限并发、WAITING_INPUT 恢复和环境告警 | 并发下是否保持单项目单 Worker 和正确终态 |

## 优化方案

### P0：完成判定和状态连续性

1. 为生产模型注册准确的上下文窗口、压缩阈值和停止行为，禁止静默 fallback；
2. 将输入附件、工作目录、恢复 thread 和 checkpoint 写入可校验 manifest；恢复时先恢复文件状态，再恢复 Codex 会话；
3. 在长操作前、失败路线后、软截止事件和最终输出前强制保存 checkpoint；
4. 将 `flag.verify` 参数校验前置：验证脚本存在、源 Artifact 属于当前项目、候选不在已拒绝集合。

### P1：减少重复探索和无效预算

1. 以项目范围保存失败路线指纹和失败原因，跨 Intent 注入上下文；
2. Observer 达到重复阈值后必须生成 `REDIRECT` 或 `ESCALATE`；
3. 将新增事实数、Artifact 增量、checkpoint 是否保存和工具失败次数纳入无进展判断；
4. 将 phase 的软超时、动作预算和路线重复阈值写入事件。

### P2：提高批量解题稳定性

1. API 集成测试使用独立临时数据库和锁目录，测试结束后显式关闭 lifespan；
2. 增加题目组并发、恢复、停止、手工 flag 拒绝、环境不可用和清理失败的组合测试；
3. 为附件下载记录开始、重试、超时、取消和人工接管事件；
4. 按题型、阶段和失败类型统计成功率，不再只报告“完成/未完成”。

## 下一轮验收标准

- 任意 Worker 超时后，至少保留一条 checkpoint 或明确的收尾失败事件；
- resume Attempt 能继承原 thread，并读取同一份输入 manifest 和最新 Blackboard；
- 同一路线达到阈值后不会再次执行相同工具请求；
- 没有靶机的 Web 项目可以继续题目描述、附件和本地分析，不会被误标为运行时失败；
- 未通过 `flag.verify` 的候选不能进入 `FLAG_READY`；验证成功必须双次运行、单 flag 输出且来源 Artifact 属于当前项目；
- 题目组并发停止或恢复后，项目和组状态不会互相覆盖；
- 在隔离数据库和锁目录下，完整测试不再出现残留 API 实例导致的锁冲突。

## 原始证据索引

以下 Artifact 可用于逐条复核。完整 transcript 不复制到本文；其中包含的候选 flag 和题目内容仍应按敏感测试数据处理。

| 现象 | Artifact |
| --- | --- |
| 超时、模型元数据 fallback、恢复后重新寻找输入 | `artifacts/proj_a4219fd6db1549da/artifact_0cd97e4af6014417.txt` |
| 超时后继续重建磁盘/图片并重复分析 | `artifacts/proj_a4219fd6db1549da/artifact_0e6856a088684942.txt` |
| 验证脚本运行成功但输出格式不符合合同 | `artifacts/proj_f60403e352bc437a/artifact_1d77a2e3355845ae.txt` |
| `flag.verify` 缺少验证脚本或源 Artifact 参数 | `artifacts/proj_f60403e352bc437a/artifact_a7179d965ac349ed.txt`、`artifacts/proj_f60403e352bc437a/artifact_f78dd5f8f96040a3.txt` |
| 双次重放成功的对照样本 | `artifacts/proj_e73873918d044428/artifact_45b7ffb2728642b5.txt` |

## 测试记录

此前只读验证中 `git diff --check` 通过。`uv run pytest -q` 得到 136 passed、19 failed；失败主要集中在共享 `/tmp/aurora_test.db` 和 API instance lock 冲突，另有一项固定 ID 测试数据重复，不能直接视为本轮业务逻辑回归。
