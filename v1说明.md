# 零奈 v1 更新文档

## 2026.8.4

### 当前已实现

1. 前端构建和基础使用；
2. 基础 Solver；
3. 调度、解题机制和 Blackboard 上下文管理；
4. Bugku 题目集测试：20 题完成 18 题。

### 已知问题

- 点击“重新思考”时有概率卡死；
- 解题过程中工具调用不足；
- 模型可能在缺少证据时猜测并提交错误 flag。

## 2026.8.11：v1.1 更新

### Fix

- 修复“重新思考”导致卡死的问题；
- 增加 flag 证据链约束，降低幻觉候选进入结果链路的概率。

### What's New

- 接入 Reverse 和 Pwn 相关工具 MCP，通过 `Aurora-Codex:heavy` 镜像提供工具；
- 将工具调用、轮次反思和 flag 验证列为后续版本重点。

### 已知问题

- Codex 作为独立 `codex exec` 子进程运行，外层 Manager 超时可能早于内部进展结束；
- Agent Loop 可能重复执行相同路线；
- Blackboard 只在回合结束后同步，下一轮可能重复已有分析；
- Web 题靶机发现和打开流程不稳定。

## 2026.8.13：v1.2 更新

### Fix

- Worker 运行期间可通过 `aurora_blackboard` MCP 实时读取事实、写入事实和保存 checkpoint；
- 记录 Codex thread/turn，支持子 Attempt 继承父 Attempt 状态并使用 `codex exec resume` 续跑；
- Worker 超时先向 Codex 发送 SIGINT，再进入收尾宽限，保留已有 transcript 和状态；
- 修复靶机发现将统计、题解和静态资源地址误识别为靶机的问题；
- 修复宿主机 `127.0.0.1`/`localhost` 代理地址错误传入 Worker 容器的问题。

### What's New

- 增加靶机候选提取、宿主机/Worker 双侧探测、可信度排序和手工确认流程；
- Web 界面增加靶机候选列表、手工指定入口和 Worker 实时事件展示；
- 增加 Worker 预检、容器回连、工作目录权限对齐、控制令牌脱敏和 core/heavy 镜像校验；
- Kali 软件源切换为中科大镜像。

### 已知问题

- 靶机自动启动和候选提取仍依赖目标平台页面结构、接口响应和异步状态；
- 非标准容器环境可能需要设置 `AURORA_WORKER_CONTROL_BASE_URL`；
- Agent Loop 的重复推理只能通过实时 Blackboard、路线指纹和会话恢复缓解。

## 2026.8.15：v1.3 更新

### Fix

- 为 Codex 结构化输出增加状态、续跑 Intent、阻塞项和工具请求的规范化，缺少可执行续跑路线时明确记录 blocker；
- 增加 Codex shell action 的 ToolTrace、动作序号、路线指纹和敏感命令脱敏；
- 增加软截止事件、动作预算、收尾宽限和终态 barrier，避免超时或 late output 覆盖已结束的 Intent；
- Scheduler 超时回收时清理 Worker 容器，并记录清理结果；
- Observer 和 Manager 增加失败路线重复检测、重定向和基于 checkpoint 的后续 Intent；
- 题目组恢复逻辑支持 WAITING_INPUT、旧版靶机阻塞项目迁移和并发执行后的状态收敛。

### What's New

- 靶机改为可选输入：没有靶机时先使用题目描述、附件和已有 Artifact 做本地分析，网络请求仍受授权策略约束；
- 题目组支持受限并发，默认全局并发上限为 2，单个项目仍保持单 Worker；
- Hands-free 附件下载增加浏览器下载读取、远程请求回退、Referer、重试和硬超时，异常附件转人工处理；
- 为不同解题阶段设置软/硬超时、动作预算、路线重复阈值和收尾宽限；
- Web UI 增加并发/环境告警和可选靶机状态展示；
- 增加无靶机项目、环境不可用继续派发、旧版靶机阻塞恢复、并发题目组和附件超时测试；
- 配置文档新增 `AURORA_DEFAULT_MAX_AGENT_ACTIONS`、`AURORA_DEFAULT_MAX_ROUTE_REPEATS`、`AURORA_DEFAULT_FINALIZE_GRACE_SECONDS`、`AURORA_MAX_CHALLENGE_GROUP_CONCURRENT` 和 `AURORA_CATALOGER_ATTACHMENT_TIMEOUT_SECONDS`。

### 已知问题

- 生产模型仍需显式注册准确的模型元数据，避免 fallback metadata 影响预算和停止判断；
- 会话恢复还需要和输入附件、工作目录 manifest 一起验证，thread 恢复不代表文件状态完整恢复；
- `flag.verify` 的脚本、源 Artifact 和候选拒绝状态仍应在派发前做完整参数校验；
- 题目组并发、手工验证、环境不可用和清理失败的组合场景仍需更多真实题目验证。
