# 零奈 v1 更新文档
## 2026.8.4
### 当前已实现:
1. 前端构建和使用；
2. 基础解题器
3. 调度、解题机制
4. 测试题目集 https://ctf.bugku.com/challenges/index/gid/2.html ，共20题，解出18题。解题正确率尚可
5. black board上下文管理

### 已知漏洞
- 点击 “重新思考” 时有概率出现卡死的情况；

### v2 目标
1. 目前解题过程中未见工具调用，增加工具调用在解题中的占比
2. 每轮结束后增加反思模块，整理上一轮的结果并给出建议
3. 解决模型幻觉问题。本次测试中有部分题目出现幻觉，提交了错误的flag

# 零奈 v1.1 更新
## 2026.8.11
### Fix
- 修复了“重新思考”时会导致卡死的问题；
### What's New
- 模型在获取flag时将严格按照证据链推导，最大可能避免幻觉；
- 接入 Reverse 和 Pwn 相关工具 MCP，现在通过构建 Aurora-Codex:heavy 启动这些工具。经实战验证可行。

### 已知问题
- Codex 对 Aurora 不可见，codex exec作为独立工具调用受30min超时限制，这会导致 codex 内存在进展但是被 Manager 直接杀掉导致任务超时；
- Codex 内部 Agent Loop 存在缺陷，导致 llm 解题时出现重复步骤的情况，消耗算力但是没有进展；
- 现存的机制为 `Blackboard → 静态 prompt 上下文 → Codex agent loop → 最终输出 → 外层 Blackboard 更新`，导致 `Blackboard` 内容无法及时同步给下一轮会话，间接导致了重复步骤问题；
- web题目靶机无法正常打开。

# 零奈 v1.2 更新
## 2026.8.13
### Fix
- Codex Worker 运行期间可通过 `aurora_blackboard` MCP 实时读取黑板、写入事实和保存检查点，解决原有黑板只在回合结束后同步的问题；
- 支持记录 Codex 会话进度、继承父 Attempt 的会话状态并使用 `codex exec resume` 续跑，减少超时或重试后重复分析；
- Worker 超时时先向 Codex 发送 SIGINT，并保留 10 秒收尾时间，避免直接终止导致已有进展丢失；
- 修复靶机发现会将统计、题解和静态资源地址误识别为靶机的问题，旧的错误靶机记录会自动失效并撤回关联事实；
- 修复宿主机代理绑定 `127.0.0.1` 或 `localhost` 时被错误传入 Worker 容器的问题。

### What's New
- 增加靶机管理流程：浏览器从页面控件和结构化响应中提取候选地址，随后同时探测宿主机与 Worker 的 DNS、TCP 和 HTTP 可达性；
- 候选靶机按照可信度保存。唯一的高可信可达地址自动激活；多个候选、付费启动或尚未就绪时暂停并要求用户确认；
- Web 界面新增靶机候选列表和手工指定靶机入口，可查看探测结果后确认激活；
- 实时展示 Codex 会话启动、推进、恢复、黑板事实和检查点等 Worker 事件；
- 增加 Worker 运行前检查、容器内主机回连、工作目录权限对齐和控制令牌脱敏；运行脚本会同时构建并校验 core、heavy 两个镜像；
- Kali 软件源切换为中科大镜像，提升国内构建可用性。

### 已知问题
- 靶机自动启动和候选地址提取仍依赖目标平台的页面结构与接口返回；遇到特殊交互或异步创建流程时，仍可能需要手工指定或确认靶机；
- Codex Agent Loop 本身的重复推理问题只能通过实时黑板、路线指纹和会话恢复缓解，仍需在真实题目中持续观察；
- Worker 回连 Aurora 控制面依赖 `host.docker.internal` 与 Docker 网关配置，非标准容器运行环境可能需要设置 `AURORA_WORKER_CONTROL_BASE_URL`。
