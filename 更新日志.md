# 零奈更新日志

## 2026.8.4

### 当前已实现

1. 前端构建和基础使用；
2. 基础 Solver；
3. 调度、解题机制和 Blackboard 上下文管理；
4. Bugku 题目集测试：20 题完成 18 题。

### 已知问题

- 点击“重新思考”时有概率卡死；
- 解题过程中工具调用不足；
- 模型可能在缺少证据时猜测并提交错误 flag。

## 2026.8.11：v1.1 更新

### Fix

- 修复“重新思考”导致卡死的问题；
- 增加 flag 证据链约束，降低幻觉候选进入结果链路的概率。

### What's New

- 接入 Reverse 和 Pwn 相关工具 MCP，通过 `Aurora-Codex:heavy` 镜像提供工具；
- 将工具调用、轮次反思和 flag 验证列为后续版本重点。

### 已知问题

- Codex 作为独立 `codex exec` 子进程运行，外层 Manager 超时可能早于内部进展结束；
- Agent Loop 可能重复执行相同路线；
- Blackboard 只在回合结束后同步，下一轮可能重复已有分析；
- Web 题靶机发现和打开流程不稳定。

## 2026.8.13：v1.2 更新

### Fix

- Worker 运行期间可通过 `aurora_blackboard` MCP 实时读取事实、写入事实和保存 checkpoint；
- 记录 Codex thread/turn，支持子 Attempt 继承父 Attempt 状态并使用 `codex exec resume` 续跑；
- Worker 超时先向 Codex 发送 SIGINT，再进入收尾宽限，保留已有 transcript 和状态；
- 修复靶机发现将统计、题解和静态资源地址误识别为靶机的问题；
- 修复宿主机 `127.0.0.1`/`localhost` 代理地址错误传入 Worker 容器的问题。

### What's New

- 增加靶机候选提取、宿主机/Worker 双侧探测、可信度排序和手工确认流程；
- Web 界面增加靶机候选列表、手工指定入口和 Worker 实时事件展示；
- 增加 Worker 预检、容器回连、工作目录权限对齐、控制令牌脱敏和 core/heavy 镜像校验；
- Kali 软件源切换为中科大镜像。

### 已知问题

- 靶机自动启动和候选提取仍依赖目标平台页面结构、接口响应和异步状态；
- 非标准容器环境可能需要设置 `AURORA_WORKER_CONTROL_BASE_URL`；
- Agent Loop 的重复推理只能通过实时 Blackboard、路线指纹和会话恢复缓解。

## 2026.8.15：v1.3 更新

### Fix

- 为 Codex 结构化输出增加状态、续跑 Intent、阻塞项和工具请求的规范化，缺少可执行续跑路线时明确记录 blocker；
- 增加 Codex shell action 的 ToolTrace、动作序号、路线指纹和敏感命令脱敏；
- 增加软截止事件、动作预算、收尾宽限和终态 barrier，避免超时或 late output 覆盖已结束的 Intent；
- Scheduler 超时回收时清理 Worker 容器，并记录清理结果；
- Observer 和 Manager 增加失败路线重复检测、重定向和基于 checkpoint 的后续 Intent；
- 题目组恢复逻辑支持 WAITING_INPUT、旧版靶机阻塞项目迁移和并发执行后的状态收敛。

### What's New

- 靶机改为可选输入：没有靶机时先使用题目描述、附件和已有 Artifact 做本地分析，网络请求仍受授权策略约束；
- 题目组支持受限并发，默认全局并发上限为 2，单个项目仍保持单 Worker；
- Hands-free 附件下载增加浏览器下载读取、远程请求回退、Referer、重试和硬超时，异常附件转人工处理；
- 为不同解题阶段设置软/硬超时、动作预算、路线重复阈值和收尾宽限；
- Web UI 增加并发/环境告警和可选靶机状态展示；
- 增加无靶机项目、环境不可用继续派发、旧版靶机阻塞恢复、并发题目组和附件超时测试；
- 配置文档新增 `AURORA_DEFAULT_MAX_AGENT_ACTIONS`、`AURORA_DEFAULT_MAX_ROUTE_REPEATS`、`AURORA_DEFAULT_FINALIZE_GRACE_SECONDS`、`AURORA_MAX_CHALLENGE_GROUP_CONCURRENT` 和 `AURORA_CATALOGER_ATTACHMENT_TIMEOUT_SECONDS`。

### 已知问题

- 生产模型仍需显式注册准确的模型元数据，避免 fallback metadata 影响预算和停止判断；
- 会话恢复还需要和输入附件、工作目录 manifest 一起验证，thread 恢复不代表文件状态完整恢复；
- `flag.verify` 的脚本、源 Artifact 和候选拒绝状态仍应在派发前做完整参数校验；
- 题目组并发、手工验证、环境不可用和清理失败的组合场景仍需更多真实题目验证。

## 2026.8.18：v1.4 更新

### Fix

- 重构 Worker 的软截止、硬截止和收尾流程：达到预算后先请求 checkpoint、向 Codex 发送 SIGINT，并在收尾宽限结束后再强制终止；终态 barrier 会丢弃迟到输出，避免已结束的 Attempt 被异步结果覆盖；
- 为 Intent、Worker 和 Attempt 增加 lease generation 校验，Scheduler 可回收失去有效租约的孤儿 Worker、清理残留容器，并把可重试 Intent 恢复为 `PENDING`；
- 完善 Codex resume 完整性验证：恢复前校验输入 manifest、工作文件、Codex 状态文件、项目归属和 SHA-256；验证失败时记录 `codex.resume_rejected` 并开启新 thread；
- 持久化 `/workspace/work` 和 Codex 状态，增加恢复文件数量、总大小和单文件大小限制，避免不完整或超量工作区进入下一轮；
- 增加无进展动作预算与实时 checkpoint 判定，减少没有新 Fact、Artifact 或 checkpoint 时的重复 shell 路线；
- 强化 `flag.verify` 参数校验，要求验证脚本和同项目源 Artifact，限制脚本大小，并拒绝重新进入验证链路的历史错误 flag；
- Worker 启动前检查 CC Switch、OpenVPN 和运行镜像状态，依赖不可用时显式预检失败，不再启动必然失败的 Solver；
- 修复 SQLite 无时区时间参与运行顺序和租约判断时的比较问题；
- 修复 TSecBench Web 地址被错误保留为 `tcp://` 的问题，常见 Web 端口会转换为 HTTP/HTTPS 授权目标；
- 修复 TSecBench 多 Flag 题在首个 Flag 正确后被提前标记完成的问题，现会保留已接受进度并继续寻找剩余 Flag；
- TSecBench 题目改为调度时按需申请靶机，不再要求导入阶段预先存在靶机或附件；跨批次最多保留 3 个活动实例，容量耗尽时等待而不是判题失败；
- 补齐 TSecBench 在题目完成、人工验证和批次停止时的环境释放，同时清除失效 target 和网络授权；
- OpenVPN 新配置采用删除旧记录后重建，替换时同时清理旧容器和临时明文文件；连接诊断可区分认证失败、TLS 协商失败与路由健康失败；
- OpenVPN 连接等待默认调整为 75 秒，且不低于 OpenVPN 2.6 默认的 60 秒 TLS 握手窗口。

### What's New

- 原生接入 TSecBench OpenAPI：支持题目列表导入、`unique_code` 去重、按需启动/关闭实例、Hint 获取、Flag 提交、累计得分和多 Flag 完成状态；
- Web 增加 TSecBench 设置面板，可在运行时配置 Base URL、Benchmark Token、请求超时和最多 3 个并发实例；Token 仅保存在当前 API 进程内存，不写入数据库或 Artifact；
- 新增容器化 OpenVPN 网关：OVPN、可选账号密码使用网页主密码加密落库；VPN 运行在独立网络命名空间，只允许显式配置的 IPv4/CIDR 经过隧道，不修改宿主机默认路由和 DNS；
- Solver Worker 可共享已连接 OpenVPN 容器的网络命名空间；隧道未连接或健康检查失败时 fail closed，不回退到宿主机直连；
- 新增项目可靠性报告接口 `GET /api/projects/{project_id}/reliability`，汇总 checkpoint 覆盖率、收尾事件、resume 验证率、孤儿 Worker、模型元数据 fallback 和 Codex 路线失败；
- 增加 API 单实例锁，避免同一数据库被多个 Aurora API 进程同时调度；
- 增加 `start.sh` 一体化入口：同步 Python 依赖、按需构建 Worker/OpenVPN/CC Switch 镜像、安装和构建 Web，然后启动私有运行时与 API；支持 `--rebuild` 强制重建；
- 新增 `AURORA_CODEX_REQUIRE_EXPLICIT_MODEL_METADATA`，生产模式要求明确配置上下文窗口和自动压缩阈值，避免未知模型静默使用 fallback；
- 新增 `AURORA_DEFAULT_MAX_NO_PROGRESS_ACTIONS`、`AURORA_RESUME_MAX_FILES`、`AURORA_RESUME_MAX_BYTES`、`AURORA_TSECBENCH_*`、`AURORA_OPENVPN_*` 和 API 监听/锁目录配置；
- Web 增加 OpenVPN 和 TSecBench 状态、配置、连接测试及错误诊断界面；文档补充一体化启动、TSecBench、OpenVPN 和可靠性验收流程。

### 验证

- 新增 TSecBench 导入、实例生命周期、并发限制、多 Flag 提交和运行时配置测试；
- 新增 OpenVPN 配置校验、加密替换、隔离网络、路由、连接诊断和 API 上传测试；
- 新增租约 fencing、孤儿回收、checkpoint 覆盖、resume manifest 完整性、无进展预算和模型元数据闸门测试；
- 当前全量自动化测试通过：191 项通过，保留 1 条 Starlette/httpx 上游弃用警告。

### 已知问题

- TSecBench 是闭源动态靶场，题目导入只包含平台元数据；Solver 必须等调度器成功申请实例地址并确认 SSLVPN 可达后才能执行网络分析；
- TSecBench 最多同时运行 3 个实例，多个批次并行时可能因容量耗尽进入等待，需要在实例释放后重新调度；
- UDP OpenVPN 在丢包或服务端限流时可能经历完整的 60 秒 TLS 重试；`VERIFY OK` 只表示服务端证书验证成功，不代表隧道已经建立；
- OpenVPN 选择新文件后仍需显式点击“保存配置”，再执行“连接”；API 重启后加密配置保持存在，但必须重新输入主密码解锁；
- OpenVPN 当前仅支持 TUN、内联证书/私钥和显式 IPv4 路由，不支持 ZIP、外部证书文件、TAP、脚本/plugin、IPv6 或服务端默认路由；
- 进程内 TSecBench Token 和已解锁的 OpenVPN 主密钥不会跨 API 重启保留；长期 Token 仍应通过环境变量提供；
- 真实比赛环境仍需继续验证 VPN 抖动、平台实例回收失败、API 重启和多批次同时恢复等组合场景。

## 2026.8.19：Flag 自主提交闭环

### What's New

- 新增服务端门禁工具 `flag.submit`，Solver 只能按 `candidate_id` 提交当前项目已通过本地证据验证的候选，不能直接传入任意 flag 字符串；
- 支持在同一批工具请求中依次执行 `flag.verify` 和 `flag.submit(candidate_id="latest_verified")`，验证成功后可立即提交平台；
- 平台接受、拒绝、多 Flag 部分接受和接口不可用均写回候选、项目及题目组状态，拒绝值和原因会进入下一轮 Solver 上下文；
- 增加重复提交、跨 Attempt 回退、原始字符串注入及平台决定被后置 Artifact 扫描覆盖的保护。

### Fix

- 修复 Codex 最终消息被 Markdown JSON 代码围栏包裹时误判为 `output_invalid_json`，现在会在结构校验前容错提取完整 JSON 对象；
- `flag.verify` 兼容模型误传的内联 Python 验证脚本，并把请求超时安全钳制到 1–60 秒；
- rethink 会重新打开未完成的失败题目及所属题组，并在独立项目续跑时保留 Slab Match 比赛上下文；
- Slab Match 根据题目规则自动选择提交完整 flag 或仅提交花括号内内容，避免正确答案因平台格式约定被拒；
- 平台接受 `flag.submit` 后同步收敛项目、题目和题组状态，并在并发题组仍有活动题目时保持 `RUNNING`；
- Slab Match 控制面拒绝携带 AccessKey 的跨域重定向，附件下载每次重定向都会重新拦截内网和 metadata 地址；
- 修复 `DERIVED_REPLAY` 大小写不一致导致可靠性与评测报告漏计验证候选的问题；
- Evaluation 会拒绝重叠运行、已有平台答题进度和未物化附件，且 5/20/25 分钟阶段预算会同步限制 Worker hard timeout；
- 清理并忽略 ffuf 运行历史；前端恢复严格 TypeScript 检查，并升级存在安全公告的 nanoid/PostCSS 间接依赖。

### 验证

- 当前全量自动化测试通过：247 项通过，保留 1 条 Starlette/httpx 上游弃用警告；前端类型检查、生产构建和 npm audit 通过。

### 当前限制与运维提示

- 项目级目标授权当前处于禁用状态：`PolicyEngine` 会放行全部网关请求，`AuthorizationScope`/`allowed_hosts`/`allowed_domains` 仅作为上下文和兼容数据，不能充当网络安全边界；原生 Codex shell 也不会按项目目标限制出口。部署时必须通过容器网络、防火墙、VPN 或代理白名单约束访问范围；
- Evaluation 的 baseline 与 candidate 必须使用彼此独立的干净 TSecBench 会话，或在两次运行之间由平台重置已接受进度；远程附件物化尚未实现，含附件套件当前会拒绝启动；
- API 仍为全开放 CORS 且没有内置认证，不应直接暴露到不受信任的公网；
- Worker 镜像内 vendored `glibc-all-in-one` 使用禁止商业使用的上游许可，分发或商业部署前需单独审查许可证。
